<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>LLDynamicLaunchScreen 设计思路</title>
      <link href="2021/03/02/LLDynamicLaunchScreen%20%E8%AE%BE%E8%AE%A1%E6%80%9D%E8%B7%AF/"/>
      <url>2021/03/02/LLDynamicLaunchScreen%20%E8%AE%BE%E8%AE%A1%E6%80%9D%E8%B7%AF/</url>
      
        <content type="html"><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;自从使用<code>LaunchScreen.storyboard</code>适配启动图之后，大家应该和我一样都遇到过这种问题，当APP使用一段时间后启动图中的图片就会显示成一团黑色，如图1所示。</p><table><thead><tr><th align="center"><img src="https://gitee.com/internetWei/images/raw/master/uPic/202102141921.jpg"></th></tr></thead><tbody><tr><td align="center">图1</td></tr></tbody></table><p>&nbsp;&nbsp;&nbsp;&nbsp;刚开始我一直以为是测试机缓存的问题，也就一直没管它。直到后来有用户使用我们的APP也遇到了这种情况，甚至我在使用<code>今日头条</code>这些APP时也出现了这种情况，才明白这不是缓存的问题。尝试过重装APP、重启手机，但是都没有效果(而且你也不可能要求每个用户都按照你的操作去做)。下面是我调研到的一些可以修复启动图显示异常的框架:</p><p>&nbsp;&nbsp;&nbsp;&nbsp;<a href="https://github.com/iversonxh/DynamicLaunchImage"><code>DynamicLaunchImage</code></a>是百度App技术开发的，但是现在好像已经没有维护了，我在测试时发现它有一个致命的问题，就是更新版本后会丢失上次设置的启动图；它的功能很简单，只实现了一个替换启动图的功能，如果要使用它来修复启动图显示异常，需要使用者手动生成正确的启动图然后替换启动图；另外它没有适配iOS13及以上系统，无法单独修改深色启动图或浅色启动图。</p><p>&nbsp;&nbsp;&nbsp;&nbsp;<a href="https://github.com/internetwei/LLDynamicLaunchScreen"><code>LLDynamicLaunchScreen</code></a>是我开发的一款可以修复与修改启动图的框架，相比较<code>DynamicLaunchImage</code>来说，它可以<strong>自动修复</strong>启动图显示异常，还可以<strong>精确的修改某1个启动图</strong>(例如单独修改浅色竖屏启动图、深色竖屏启动图、浅色横屏启动图、深色横屏启动图)而不影响其他启动图，支持替换iPad启动图，但是并不完美，接下来会完善它并适配其他平台(mac、iWatch、appleTV)。最重要的是我会维护它并且适配未来的iOS版本，如果您在使用过程中有遇到任何问题也可以<a href="https://github.com/internetWei/LLDynamicLaunchScreen/issues">向我提问</a>。</p><blockquote><p>上面提到的2个框架都可以修复启动图显示异常，而且都支持动态修改启动图。不过建议使用<a href="https://github.com/internetwei/LLDynamicLaunchScreen"><code>LLDynamicLaunchScreen</code></a>，因为我会继续维护它。接下来我会讲一下<code>LLDynamicLaunchScreen</code>的设计灵感与思路，如果您对它没有兴趣可以跳过。</p></blockquote><h3 id="LLDynamicLaunchScreen灵感"><a href="#LLDynamicLaunchScreen灵感" class="headerlink" title="LLDynamicLaunchScreen灵感"></a>LLDynamicLaunchScreen灵感</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;有一次在查看沙盒文件夹时无意看到了一个文件夹下面有4张图片，打开后发现其实就是APP的启动图，那个时候就想能不能在运行时动态修改这些图片的源数据来达到替换启动图的目的，测试发现确实可以，不过当时觉得这个功能比较鸡肋就搁置了。直到苹果强制要求使用<code>LaunchScreen.storyboard</code>适配启动图后，发现启动图经常显示异常然后就有了<a href="https://github.com/internetwei/LLDynamicLaunchScreen"><code>LLDynamicLaunchScreen</code></a>和这篇文章。</p><h3 id="LLDynamicLaunchScreen遇到的坑"><a href="#LLDynamicLaunchScreen遇到的坑" class="headerlink" title="LLDynamicLaunchScreen遇到的坑"></a>LLDynamicLaunchScreen遇到的坑</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;替换启动图的原理其实就是将图片写入本地的沙盒路径下就可以了，但是在开发的过程中还是遇到了很多的问题，有些问题上网找到了解决方案，有些问题上网压根找不到答案。例如在通过<code>LaunchScreen.storyboard</code>生成与当前系统主题模式相反的启动图时就一直失败，最后是通过<code>isa-swizzling</code>实现了想要的效果。</p><h3 id="LLDynamicLaunchScreen实现细节"><a href="#LLDynamicLaunchScreen实现细节" class="headerlink" title="LLDynamicLaunchScreen实现细节"></a>LLDynamicLaunchScreen实现细节</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;这里只作简单的描述，想看具体的实现细节可以<a href="https://github.com/internetWei/LLDynamicLaunchScreen">下载查看源码</a>。</p><ol><li>在<code>load</code>类方法中注册<code>UIApplicationDidFinishLaunchingNotification</code>和<code>UIWindowDidBecomeKeyNotification</code>这2个通知事件<code>didFinishLaunching</code>和<code>didBecomeKey:</code>。</li><li>在<code>didFinishLaunching</code>方法中会执行初始化、替换上次修改的启动图、修复启动图这3个操作。</li><li>在<code>didBecomeKey:</code>方法中会根据<code>LaunchScreen.storyboard</code>生成APP的启动图，关于生成启动图这里遇到了一些麻烦，<a href="#jump1">关于生成启动图的细节</a>。</li><li>替换启动图时会使用<code>moveItemAtPath: toPath: error:</code>移动系统启动图文件夹到一个临时文件夹，然后进行读取和写入，最后再移动到原来的路径下。</li></ol><h3 id="LLDynamicLaunchScreen生成启动图的细节"><a href="#LLDynamicLaunchScreen生成启动图的细节" class="headerlink" title="LLDynamicLaunchScreen生成启动图的细节"></a><span id="jump1">LLDynamicLaunchScreen生成启动图的细节</span></h3><p>&nbsp;&nbsp;&nbsp;&nbsp;如何根据<code>storyboard</code>文件生成UIImage这里就不详细赘述了。当我在生成和系统当前主题模式相反的启动图时一直生成失败，例如当前系统是浅色模式，在生成深色启动图的时候获取到的还是浅色主题下的页面。但是通过用户手动触发生成启动图又是正常的，例如在APP启动后通过用户点击按钮触发生成启动图是可以的。<br>&nbsp;&nbsp;&nbsp;&nbsp; 最初的想法是延迟执行，但是测试发现无效。最后测试发现将逻辑放在第1个<code>UIViewController</code>的<code>viewDidAppear:</code>后执行可以达到想要的效果。最初的版本是通过<code>method_exchangeImplementations</code>交换系统的<code>viewDidAppear:</code>来达到目的，但是这样做有一个弊端，当使用者也使用<code>method_exchangeImplementations</code>交换了<code>viewDidAppear:</code>时会发生一些不可预知的问题。目前的版本是使用<code>isa-swizzling</code>来实现效果。关于<code>isa-swizzling</code>可以上网搜索KVO的具体实现细节获得。使用这种方式也能达到一样的效果，而且不影响使用者在工程中使用method_exchangeImplementations<code>交换</code>viewDidAppear:`。</p><h3 id="LLDynamicLaunchScreen替换启动图样例"><a href="#LLDynamicLaunchScreen替换启动图样例" class="headerlink" title="LLDynamicLaunchScreen替换启动图样例"></a>LLDynamicLaunchScreen替换启动图样例</h3><p><img src="https://gitee.com/internetWei/images/raw/master/uPic/202102151626.gif"></p>]]></content>
      
      
      
        <tags>
            
            <tag> iOS </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
